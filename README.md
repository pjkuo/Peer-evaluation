完全在 GitHub Pages 上執行（不依賴 Firebase 後端），評分後立即計算總分並即時輸出畫面，且能加總計分、完整彙整於 HTML 中。
以下提供一個 純前端 + localStorage 儲存 的版本，支援：

三個科系切換（環工1A、植醫1A、養殖1A）

每個小組卡片顯示即時平均分與評分人數

每人每組僅能評分一次（使用隨機產生的評分者 ID 儲存於 localStorage）

評分後立即更新畫面，所有資料存在瀏覽器中

「匯出統計報表」功能（CSV）

額外提供「總分加總與排名」區域（顯示各組加總分數、平均分數，並依平均排序）
